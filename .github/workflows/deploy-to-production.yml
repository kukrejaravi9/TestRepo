name: Build and Deploy - Production
on:    
  workflow_dispatch:
    inputs:
      import_destination:
        description: 'Some input'
        required: true
        type: choice
        options: 
          - 'DEV'          
          - 'PROD'
          - 'TEST'       

  schedule: 
    - cron: '* * * * *'

jobs:
  call-reusable-job: 
    runs-on: ubuntu-latest
    steps:    
    - name: Checkout
      uses: actions/checkout@v4.1.7                
    - name: Create github app token 
      env:
        import_destination: ${{ inputs.import_destination || 'Overridden' }}
      run: |
        node -v
        node myscript.js
      
